<Window x:Class="WpMyApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="600" Width="900">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Users Section -->
        <GroupBox Grid.Row="0" Grid.Column="0" Header="Users Management" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <StackPanel Width="120" Margin="0,0,10,0">
                    <TextBlock Text="First Name" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedUser.FirstName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Width="120" Margin="0,0,10,0">
                    <TextBlock Text="Last Name" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedUser.LastName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Width="150" Margin="0,0,10,0">
                    <TextBlock Text="Email" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedUser.Email, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Width="80" Margin="0,0,10,0">
                    <TextBlock Text="Age" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedUser.Age, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel VerticalAlignment="Bottom">
                    <Button Content="Save" Command="{Binding SaveUserCommand}" 
                            Width="60" Margin="0,0,5,0"/>
                    <Button Content="Delete" Command="{Binding DeleteUserCommand}" 
                            Width="60" Background="LightCoral"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Products Section -->
        <GroupBox Grid.Row="0" Grid.Column="2" Header="Products Management">
            <StackPanel>
                <Button Content="Load Products" Command="{Binding LoadProductsCommand}"
                        Width="100" HorizontalAlignment="Left"/>
            </StackPanel>
        </GroupBox>

        <!-- Users List -->
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Users List">
            <DataGrid ItemsSource="{Binding Users}" 
                      SelectedItem="{Binding SelectedUser}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="100"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="100"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                    <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="60"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Products List -->
        <GroupBox Grid.Row="1" Grid.Column="2" Header="Products List">
            <DataGrid ItemsSource="{Binding Products}" 
                      SelectedItem="{Binding SelectedProduct}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="120"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C2}" Width="80"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="60"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Status -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Load Users" Command="{Binding LoadUsersCommand}" 
                    Width="80" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                       FontWeight="Bold" Foreground="DarkBlue"/>
        </StackPanel>
    </Grid>
</Window>